
Config = {}

-- Key bindings (default keys)
Config.Keys = {
    Emergency = 38, -- E key
    Duress = 44,    -- D key
    Backup = 29,    -- B key
    Location = 177  -- END key
}

-- Sound settings
Config.Sounds = {
    EnableSounds = true,
    Volume = 0.5
}

-- Display settings
Config.Display = {
    ShowCallerNumber = true,
    NotificationDuration = 30000, -- Duration in ms
    EnableFlashing = true
}
